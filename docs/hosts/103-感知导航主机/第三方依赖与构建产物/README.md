# 第三方依赖与构建产物

本目录界定 103 主机中不应按自研业务代码递归修改的 SDK、上游组件、构建产物和运行时目录。

| 文件 | 内容 | 使用方式 |
| --- | --- | --- |
| [第三方依赖与构建产物.md](第三方依赖与构建产物.md) | 驱动 SDK、导航/建图上游组件、`build`/`devel`/`install` 等目录说明 | 升级 SDK、重建工作区或清理磁盘前阅读。 |

清理或覆盖上述目录前，应先准备可重复的构建流程并记录 ROS、CUDA/TensorRT、DDS 与设备固件版本。
